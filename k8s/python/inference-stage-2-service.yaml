apiVersion: v1
kind: Service
metadata:
  name: inference-stage-2
  namespace: inference
spec:
  selector:
    app: inference-stage-2
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
