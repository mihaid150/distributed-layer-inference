apiVersion: v1
kind: Service
metadata:
  name: inference-stage-3
  namespace: inference
spec:
  selector:
    app: inference-stage-3
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
