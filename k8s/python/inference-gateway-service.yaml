apiVersion: v1
kind: Service
metadata:
  name: inference-gateway
  namespace: inference
spec:
  type: NodePort
  selector:
    app: inference-gateway
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080
