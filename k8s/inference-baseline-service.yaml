apiVersion: v1
kind: Service
metadata:
  name: inference-baseline
  namespace: inference
spec:
  type: NodePort
  selector:
    app: inference-baseline
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30081
